import { PaginationListClass } from './pagination-list.class';

describe('PaginationListClass', () => {
  it('should create an instance', () => {
    expect(new PaginationListClass()).toBeTruthy();
  });
});
